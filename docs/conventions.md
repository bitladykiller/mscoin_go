# Conventions

- Keep HTTP contracts in `idl/api`.
- Keep RPC contracts in `idl/rpc`.
- Keep SQL in `internal/repository`.
- Keep orchestration rules in `internal/domain/service`.
- Keep transport adaptation in `internal/logic`.
- Keep detailed comments on non-trivial models, workflows, cache keys, and
  consumer logic.
